---
phase: 09-portfolio-search
task: verification
total_tasks: 3 plans complete, verification pending
status: blocked
last_updated: 2026-03-05T02:36:29.432Z
---

<current_state>
Phase 09 execution completed all 3 plans (09-01, 09-02, 09-03). Verification ran and returned `human_needed`. During human testing, user found two bugs:

1. Connect Wallet button is not clickable
2. Markets list doesn't show without connecting a wallet

Multiple fix attempts were made but all made things worse. ALL changes were reverted to the exact post-execution state (commit 3b909b5 + phase docs commits). The codebase is back to the state right after phase 9 plans finished executing.
</current_state>

<completed_work>

- Plan 09-01: Portfolio page with useUserPositions hook, Active/Claimable/History sections — DONE
- Plan 09-02: Header search bar with as-you-type dropdown, keyboard nav, status badges — DONE
- Plan 09-03: Mobile responsiveness — hamburger menu, search overlay, sticky bet bar, touch targets — DONE
- Verification: Ran, status `human_needed`, 19/19 automated checks passed
- SUMMARY.md files created for all 3 plans
- VERIFICATION.md created at `.planning/phases/09-portfolio-search/09-VERIFICATION.md`
</completed_work>

<remaining_work>

- Fix bug: Connect Wallet button not clickable (likely the gradient backdrop div in Header.tsx missing `pointer-events-none`, but needs careful testing)
- Fix bug: Markets not loading without wallet connected (root cause: `__root.tsx` wraps everything in `ClientOnly` — the SSR fallback `FallbackLayout` renders `<Outlet />` WITHOUT `ConnectionProvider`, so `useMarkets()` → `useReadOnlyProgram()` → `useConnection()` returns invalid context)
- After fixes: re-run human verification for the 5 items
- After verification passes: complete phase (update ROADMAP, STATE, offer next)
</remaining_work>

<decisions_made>

- The wallet adapter packages (`WalletProvider`, `WalletModalProvider`) ARE SSR-safe — verified by reading source code. They use `typeof window` guards and `globalThis.navigator?.userAgent` with optional chaining.
- `useWallet()` without a `WalletProvider` does NOT throw — it logs `console.error` and returns defaults (`publicKey: null`, `connected: false`). The errors in the terminal are warnings, not crashes.
- `ClientOnly` from `@tanstack/react-router` DOES work — uses `useSyncExternalStore(subscribe, () => true, () => false)`. Renders fallback during SSR, children on client.
- Attempted approaches that FAILED: (1) removing ClientOnly entirely → hydration issues, (2) useMounted pattern → blank page because TanStack Start needs Outlet in SSR tree, (3) ssr:false on root route → also broke rendering
</decisions_made>

<blockers>
- The `ClientOnly` + SSR interaction in TanStack Start is tricky. The fallback renders without wallet providers, causing console warnings. The switch from fallback to children may cause hydration issues.
- The correct fix likely involves: (a) adding `pointer-events-none` to Header gradient backdrop, (b) moving `ConnectionProvider` outside `ClientOnly` so the fallback has RPC access, (c) keeping `WalletButton` inside `ClientOnly` in the Header with its non-interactive fallback, OR finding a way to make the wallet button work in the fallback. Test EACH change individually against `bun dev` before committing.
</blockers>

<context>
The root layout (`__root.tsx`) uses `ClientOnly` to wrap all Solana providers. During SSR, a `FallbackLayout` renders Header + Outlet without any providers. After client hydration, `ClientOnly` switches to the full provider tree. The key insight is that `ClientOnly` works but the fallback path is missing providers that components need. The fix needs to be surgical — change ONE thing at a time and test with `bun dev` to avoid cascading breakage. Start the dev server with `cd app && bun dev`, test in browser at localhost:3000.
</context>

<next_action>
Start with: Run `bun dev` and confirm the app loads in its current (reverted) state. Then fix ONE issue at a time:
1. First: add `pointer-events-none` to the gradient backdrop div in Header.tsx line ~40. Test if Connect Wallet becomes clickable.
2. Second: move `ConnectionProvider` outside `ClientOnly` in __root.tsx (keep WalletProvider + WalletModalProvider inside). Test if markets load.
3. Test each change individually in the browser before moving to the next.
</next_action>
