---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/package.json
  - app/vite.config.ts
  - app/tsconfig.json
  - app/postcss.config.js
  - app/src/styles/app.css
  - app/src/router.tsx
  - app/src/routes/__root.tsx
  - app/src/routes/index.tsx
  - app/src/routes/market/$id.tsx
  - app/src/routes/portfolio.tsx
  - app/src/components/Header.tsx
autonomous: true
requirements:
  - INF-05

must_haves:
  truths:
    - "TanStack Start dev server starts without errors on `bun run dev`"
    - "Homepage route (/) renders a placeholder page with Avenir branding"
    - "Market detail route (/market/:id) renders with dynamic id parameter"
    - "Portfolio route (/portfolio) renders a placeholder page"
    - "Header component with nav links and wallet connect placeholder is visible on all pages"
    - "Tailwind v4 utility classes are applied and visible in the rendered output"
    - "shadcn/ui Button component renders correctly on at least one page"
  artifacts:
    - path: "app/package.json"
      provides: "Frontend dependencies: TanStack Start, Tailwind v4, shadcn/ui, @solana/web3.js v1, @coral-xyz/anchor 0.32.1"
      contains: "@tanstack/react-start"
    - path: "app/vite.config.ts"
      provides: "Vite config with tanstackStart and tailwindcss plugins"
      contains: "tanstackStart"
    - path: "app/src/styles/app.css"
      provides: "Tailwind v4 CSS entry with @import and minimal @theme tokens"
      contains: "@import \"tailwindcss\""
    - path: "app/src/routes/__root.tsx"
      provides: "Root layout with Header, Outlet, Scripts"
      contains: "createRootRoute"
    - path: "app/src/routes/index.tsx"
      provides: "Homepage route component"
      contains: "createFileRoute"
    - path: "app/src/routes/market/$id.tsx"
      provides: "Market detail route with dynamic param"
      contains: "Route.useParams"
    - path: "app/src/routes/portfolio.tsx"
      provides: "Portfolio route component"
      contains: "createFileRoute"
    - path: "app/src/components/Header.tsx"
      provides: "Header component with nav and wallet connect slot"
      contains: "Header"
  key_links:
    - from: "app/src/routes/__root.tsx"
      to: "app/src/components/Header.tsx"
      via: "import and render in root layout"
      pattern: "import.*Header"
    - from: "app/src/routes/__root.tsx"
      to: "app/src/styles/app.css"
      via: "CSS import for Tailwind styles"
      pattern: "import.*app\\.css"
    - from: "app/vite.config.ts"
      to: "@tanstack/react-start/plugin/vite"
      via: "tanstackStart plugin registration"
      pattern: "tanstackStart"
---

<objective>
Scaffold the TanStack Start frontend application in the `app/` subdirectory within the Arcium project root. Set up file-based routing with placeholder pages for homepage, market detail, and portfolio. Configure Tailwind v4 with CSS-first @theme tokens and integrate shadcn/ui components.

Purpose: Creates the frontend shell that Phase 4 (Design System & Fog) and Phase 7 (Core UI Integration) build upon. Proves the TanStack Start + Tailwind v4 + shadcn/ui stack works together before investing in design or data integration.

Output: A running TanStack Start app with three routes, a shared header layout, Tailwind v4 styling, and at least one shadcn/ui component rendered.
</objective>

<execution_context>
@/Users/laitsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laitsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TanStack Start project with Tailwind v4 and shadcn/ui</name>
  <files>
    app/package.json, app/vite.config.ts, app/tsconfig.json, app/postcss.config.js, app/src/styles/app.css
  </files>
  <action>
Create the TanStack Start app in the `app/` directory. Use bun as the package manager (per user decision).

Option A (preferred): Use the scaffolding command:
```bash
cd /Users/laitsky/Developments/avenir
bun create @tanstack/start@latest app --tailwind --add-ons shadcn
```

If the CLI doesn't support `--tailwind --add-ons shadcn` flags or creates issues, use Option B:

Option B (manual setup):
1. `mkdir -p app && cd app`
2. `bun init`
3. Install core deps:
   ```bash
   bun add @tanstack/react-start @tanstack/react-router react react-dom
   bun add -d @types/react @types/react-dom typescript vite @tailwindcss/vite
   ```
4. Install shadcn/ui: follow https://ui.shadcn.com/docs/installation/tanstack for TanStack Start
5. Install Solana client deps (for future use, installed now to validate compatibility):
   ```bash
   bun add @solana/web3.js@1 @coral-xyz/anchor@0.32.1 @arcium-hq/client@0.5.2
   ```

**vite.config.ts** (use Vite-based config, NOT the deprecated Vinxi app.config.ts):
```typescript
import { defineConfig } from "vite";
import { tanstackStart } from "@tanstack/react-start/plugin/vite";
import tailwindcss from "@tailwindcss/vite";

export default defineConfig({
  plugins: [
    tanstackStart(),
    tailwindcss(),
  ],
});
```

**app.css** (Tailwind v4 CSS-first configuration -- NO tailwind.config.js):
```css
@import "tailwindcss";

@theme {
  /* Minimal tokens for Phase 1 — full design system in Phase 4 */
  --color-background: #0a0a0a;
  --color-foreground: #fafafa;
}
```

Ensure the CSS file is imported in the root layout (`__root.tsx`).

**tsconfig.json**: Configure path aliases:
- `@/*` maps to `./src/*`
- `@/idl` maps to `../target/idl/` (for future Anchor IDL consumption)
- `@/types` maps to `../target/types/` (for future Anchor type consumption)
  </action>
  <verify>
    <automated>cd /Users/laitsky/Developments/avenir/app && bun run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - TanStack Start project exists in `app/` with package.json
    - vite.config.ts uses tanstackStart() and tailwindcss() plugins (NOT Vinxi app.config.ts)
    - Tailwind v4 configured via CSS @import + @theme (NO tailwind.config.js)
    - @solana/web3.js@1, @coral-xyz/anchor@0.32.1, @arcium-hq/client@0.5.2 installed
    - tsconfig.json has path aliases for @/*, @/idl, @/types
    - `bun run build` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Create route shells and Header component</name>
  <files>
    app/src/router.tsx,
    app/src/routes/__root.tsx,
    app/src/routes/index.tsx,
    app/src/routes/market/$id.tsx,
    app/src/routes/portfolio.tsx,
    app/src/components/Header.tsx
  </files>
  <action>
Set up TanStack Start file-based routing with placeholder pages.

**Header component** (`app/src/components/Header.tsx`):
- Avenir logo/text on the left
- Nav links: Home, Portfolio
- Wallet connect placeholder button on the right (use shadcn/ui Button component with "Connect Wallet" text, non-functional for now)
- Use Tailwind v4 utility classes for layout (flex, justify-between, items-center, etc.)
- Dark background styling consistent with --color-background

**Root layout** (`app/src/routes/__root.tsx`):
```tsx
import { createRootRoute, Outlet, HeadContent, Scripts } from "@tanstack/react-router";
import { Header } from "../components/Header";
import appCss from "../styles/app.css?url";

export const Route = createRootRoute({
  component: RootLayout,
  head: () => ({
    links: [{ rel: "stylesheet", href: appCss }],
  }),
});

function RootLayout() {
  return (
    <html lang="en">
      <head>
        <meta charSet="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Avenir - Encrypted Prediction Markets</title>
        <HeadContent />
      </head>
      <body className="bg-background text-foreground min-h-screen">
        <Header />
        <main className="container mx-auto px-4 py-8">
          <Outlet />
        </main>
        <Scripts />
      </body>
    </html>
  );
}
```

**Homepage** (`app/src/routes/index.tsx`):
- `createFileRoute("/")`
- Renders: "Avenir" heading, "Encrypted prediction markets on Solana" subtitle
- Placeholder grid area where market cards will go (Phase 4/7)

**Market detail** (`app/src/routes/market/$id.tsx`):
- `createFileRoute("/market/$id")`
- Uses `Route.useParams()` to extract `id`
- Renders: "Market #{id}" heading, placeholder areas for question, sentiment, bet placement

**Portfolio** (`app/src/routes/portfolio.tsx`):
- `createFileRoute("/portfolio")`
- Renders: "Portfolio" heading, placeholder for positions list

**Router** (`app/src/router.tsx`):
- `createRouter` with the auto-generated `routeTree`
- Export for use in the app entry

Each placeholder page should have enough structure to confirm routing works but minimal content. Use at least one shadcn/ui component (Button) in the Header to confirm the integration.
  </action>
  <verify>
    <automated>cd /Users/laitsky/Developments/avenir/app && bun run dev &amp; sleep 5 && curl -s http://localhost:3000 | head -50 && kill %1 2>/dev/null; true</automated>
  </verify>
  <done>
    - __root.tsx renders Header and Outlet on all pages
    - index.tsx renders at / with Avenir heading
    - market/$id.tsx renders at /market/123 with dynamic id
    - portfolio.tsx renders at /portfolio
    - Header has nav links (Home, Portfolio) and wallet connect placeholder Button
    - Tailwind utility classes visibly applied (dark background, text colors)
    - shadcn/ui Button renders in the Header
    - `bun run dev` starts the dev server without errors
  </done>
</task>

</tasks>

<verification>
1. `bun run dev` in `app/` starts TanStack Start dev server without errors
2. Navigating to http://localhost:3000 shows the homepage with Header
3. Navigating to http://localhost:3000/market/123 shows Market #123
4. Navigating to http://localhost:3000/portfolio shows Portfolio page
5. Header is visible on all pages with nav links and Connect Wallet button
6. Tailwind v4 styles are applied (dark background, text colors from @theme)
7. shadcn/ui Button component renders in Header
8. `bun run build` completes without errors
</verification>

<success_criteria>
- TanStack Start app runs locally in `app/` directory
- File-based routing works for /, /market/$id, /portfolio
- Tailwind v4 CSS-first configuration applied with @theme tokens
- shadcn/ui integrated with at least one component (Button) rendering
- Header with nav and wallet connect placeholder visible on all routes
- Solana client dependencies installed and compatible (@solana/web3.js@1, @coral-xyz/anchor@0.32.1)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
