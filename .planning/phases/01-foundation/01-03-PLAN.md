---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - Arcium.toml
  - encrypted-ixs/src/lib.rs
autonomous: true
requirements:
  - INF-01

must_haves:
  truths:
    - "Arcium CLI is installed and `arcium --version` returns a version string"
    - "`arcium build` compiles the placeholder encrypted instruction without errors"
    - "Arcium.toml is correctly configured with the program ID and cluster settings"
    - "encrypted-ixs/ directory has the expected structure for MPC circuit development"
  artifacts:
    - path: "Arcium.toml"
      provides: "Arcium toolchain configuration linking to the Solana program"
      contains: "program_id"
    - path: "encrypted-ixs/src/lib.rs"
      provides: "Placeholder encrypted instruction (no-op) that compiles with arcium build"
      contains: "encrypted"
  key_links:
    - from: "Arcium.toml"
      to: "Anchor.toml"
      via: "Shared program_id configuration"
      pattern: "program_id"
    - from: "encrypted-ixs/src/lib.rs"
      to: "programs/avenir/src/lib.rs"
      via: "MPC circuits reference the Solana program for callbacks"
      pattern: "avenir"
---

<objective>
Validate the Arcium toolchain installation and project configuration. Ensure `arcium build` compiles the placeholder encrypted instruction, confirm the project structure is correct for MPC circuit development in Phase 3, and verify Docker is available for `arcium test`.

Purpose: De-risks the Arcium integration early. Phase 3 (Arcium MPC Core) is the highest-risk phase in the project. Validating the toolchain now ensures the foundation is solid before attempting encrypted state relay and MPC circuits.

Output: A validated Arcium project structure where `arcium build` succeeds, Docker connectivity is confirmed, and the encrypted-ixs/ directory is ready for Phase 3 circuit development.
</objective>

<execution_context>
@/Users/laitsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laitsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate Arcium CLI and project structure</name>
  <files>
    Arcium.toml, encrypted-ixs/src/lib.rs
  </files>
  <action>
Validate the Arcium toolchain installation and project configuration created in Plan 01-01.

1. **Verify Arcium CLI installation:**
   ```bash
   arcium --version
   ```
   If not installed, install via:
   ```bash
   curl --proto '=https' --tlsv1.2 -sSfL https://install.arcium.com/ | bash
   ```
   Then source the shell profile and verify.

2. **Verify Arcium.toml configuration:**
   - Check that `Arcium.toml` exists at project root
   - Ensure it references the correct program ID (matching Anchor.toml and declare_id!)
   - Verify cluster and other settings are appropriate for localnet

3. **Verify encrypted-ixs/ structure:**
   - `encrypted-ixs/src/lib.rs` should exist (created by `arcium init`)
   - If it contains only a placeholder, update it to be a minimal valid encrypted instruction that compiles:
   ```rust
   // Placeholder encrypted instruction for Arcium MPC
   // Real circuits (update_pool, compute_payouts, etc.) will be implemented in Phase 3
   ```
   The exact content depends on what `arcium init` generated. If it generated a hello-world example, leave it. If empty, add the minimal structure needed for `arcium build` to succeed.

4. **Run `arcium build`:**
   ```bash
   cd /Users/laitsky/Developments/avenir
   arcium build
   ```
   This should compile the Solana program AND the encrypted instruction. Verify no errors.

5. **Verify Docker availability** (needed for `arcium test` in Phase 3):
   ```bash
   docker --version
   docker ps
   ```
   Log the Docker status. If Docker is not running, note this as a prerequisite for Phase 3 but do not block — `arcium build` does not require Docker, only `arcium test` does.

6. **Document the Arcium version installed:**
   Note the exact version from `arcium --version` output. If it differs from v0.4.0 (specified in CONTEXT.md), document the actual version — the research noted v0.4.0 may be outdated and `arcup install` gets latest.
  </action>
  <verify>
    <automated>cd /Users/laitsky/Developments/avenir && arcium --version && arcium build 2>&1 | tail -20 && echo "---" && ls encrypted-ixs/src/</automated>
  </verify>
  <done>
    - `arcium --version` returns a version string
    - `arcium build` compiles without errors (both Solana program and encrypted instructions)
    - Arcium.toml is configured with correct program ID matching Anchor.toml
    - encrypted-ixs/src/lib.rs exists and compiles
    - Docker availability checked and status documented
    - Actual Arcium version documented (may differ from v0.4.0 in requirements)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify end-to-end build and document toolchain versions</name>
  <files>
    Arcium.toml
  </files>
  <action>
Run a full end-to-end build verification to confirm the entire toolchain works together.

1. **Clean build from scratch:**
   ```bash
   cd /Users/laitsky/Developments/avenir
   anchor clean
   arcium build
   ```

2. **Verify all toolchain versions and document them:**
   ```bash
   rustc --version
   solana --version
   anchor --version
   arcium --version
   ```
   Expected: Rust >= 1.89.0, Solana CLI 2.3.0, Anchor 0.32.1, Arcium latest.

3. **Run the Anchor test suite to ensure nothing broke:**
   ```bash
   anchor test
   ```
   All tests from Plan 01-01 should still pass after `arcium build`.

4. **Verify the generated artifacts:**
   - `target/deploy/avenir-keypair.json` exists
   - `target/idl/avenir.json` exists (Anchor IDL)
   - `target/types/avenir.ts` exists (Anchor TypeScript types)
   - Program ID in `declare_id!`, `Anchor.toml`, and `Arcium.toml` all match

5. **If Arcium.toml program_id is out of sync**, update it to match the Anchor-generated program ID.

This task is a comprehensive smoke test ensuring the Solana program scaffold (Plan 01-01) and the Arcium project structure are fully compatible and ready for Phase 3 circuit development.
  </action>
  <verify>
    <automated>cd /Users/laitsky/Developments/avenir && anchor test 2>&1 | tail -15 && echo "---" && ls target/idl/ target/types/ target/deploy/ 2>&1</automated>
  </verify>
  <done>
    - `arcium build` followed by `anchor test` passes all tests
    - Program ID is consistent across declare_id!, Anchor.toml, and Arcium.toml
    - target/idl/avenir.json and target/types/avenir.ts are generated
    - All toolchain versions documented: Rust, Solana CLI, Anchor, Arcium
    - Clean build from scratch succeeds without errors
  </done>
</task>

</tasks>

<verification>
1. `arcium --version` returns a valid version string
2. `arcium build` compiles both the Solana program and encrypted instructions without errors
3. `anchor test` still passes after arcium build
4. Program ID consistent across all config files (declare_id!, Anchor.toml, Arcium.toml)
5. Docker status verified and documented for Phase 3
6. All toolchain versions documented (Rust, Solana, Anchor, Arcium)
</verification>

<success_criteria>
- Arcium CLI installed and functional
- `arcium build` compiles the complete project (Solana program + encrypted-ixs)
- encrypted-ixs/ directory ready for Phase 3 MPC circuit development
- Toolchain versions documented and consistent
- No regressions in Anchor tests after Arcium integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
