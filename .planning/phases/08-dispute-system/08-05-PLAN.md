---
phase: 08-dispute-system
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - tests/dispute.ts
autonomous: true
requirements:
  - RES-02
  - RES-03
  - RES-04
  - RES-05
  - RES-06

must_haves:
  truths:
    - "Integration tests validate the full dispute lifecycle: register -> approve -> escalate -> vote -> finalize -> payout"
    - "Grace period enforcement is tested (creator can resolve within 48h, rejected after)"
    - "Resolver registration and staking flows are tested"
    - "Encrypted jury voting via MPC is tested end-to-end"
    - "Dispute finalization and market resolution via tally is tested"
  artifacts:
    - path: "tests/dispute.ts"
      provides: "Integration tests for full dispute system lifecycle"
      contains: "describe.*Dispute"
      min_lines: 200
  key_links:
    - from: "tests/dispute.ts"
      to: "programs/avenir/src/lib.rs"
      via: "Calls all dispute instructions via Anchor test client"
      pattern: "program.methods"
---

<objective>
Write integration tests for the complete dispute system lifecycle covering resolver management, grace period enforcement, dispute escalation, encrypted voting, and finalization.

Purpose: Validates that all dispute system components work together end-to-end, proving the encrypted voting pipeline and market resolution via dispute.

Output: Comprehensive test file covering all dispute flows and edge cases.
</objective>

<execution_context>
@/Users/laitsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laitsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dispute-system/08-CONTEXT.md
@.planning/phases/08-dispute-system/08-01-SUMMARY.md
@.planning/phases/08-dispute-system/08-02-SUMMARY.md
@.planning/phases/08-dispute-system/08-03-SUMMARY.md
@.planning/phases/08-dispute-system/08-04-SUMMARY.md

@tests/avenir.ts
@programs/avenir/src/lib.rs
@programs/avenir/src/state/dispute.rs
@programs/avenir/src/state/dispute_tally.rs
@programs/avenir/src/state/resolver.rs
@programs/avenir/src/state/resolver_registry.rs

<interfaces>
<!-- Test patterns from existing test files -->

From tests/avenir.ts (existing test patterns -- consult for Anchor test setup, PDA derivation, MPC helpers):
- Program initialization and market creation setup
- SPL Token test helpers (createMint, createAccount, mintTo)
- PDA derivation patterns: PublicKey.findProgramAddressSync(seeds, program.programId)
- MPC computation awaiting patterns (if any from Phase 3/5 tests)

Key PDA seeds for dispute system:
- Resolver: [b"resolver", resolver_wallet.toBuffer()]
- ResolverRegistry: [b"resolver_registry"]
- Resolver vault: [b"resolver_vault", resolver_wallet.toBuffer()]
- Dispute: [b"dispute", market_id_le_bytes]
- DisputeTally: [b"dispute_tally", market_id_le_bytes]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement resolver pool and grace period tests</name>
  <files>tests/dispute.ts</files>
  <action>
Create `tests/dispute.ts` with test setup and resolver management tests.

**Test setup:**
- Initialize program config (admin, USDC mint, fee recipient).
- Create USDC mint and token accounts for admin, creator, 8+ resolvers, and 2 bettors.
- Create a market with a short deadline suitable for testing.
- Place bets from 2 bettors (so they have UserPositions for dispute eligibility).
- Derive all PDAs needed for dispute tests.

**Test cases (resolver pool - RES-04):**

1. **"Register resolver with 500 USDC minimum"** -- register_resolver with 500 USDC, verify Resolver PDA created, staked_amount correct, approved=false.

2. **"Reject registration below minimum stake"** -- register_resolver with 499 USDC, expect StakeTooLow error.

3. **"Admin approves resolver"** -- approve_resolver, verify resolver.approved=true, resolver added to ResolverRegistry.

4. **"Reject approval by non-admin"** -- approve_resolver from non-admin wallet, expect Unauthorized error.

5. **"Resolver can top up stake"** -- stake_resolver with additional USDC, verify staked_amount increases.

6. **"Register and approve 7+ resolvers"** -- Register and approve at least 7 resolvers (needed for jury selection in later tests).

**Test cases (grace period - RES-02):**

7. **"Creator can resolve within grace period"** -- Since we can't fast-forward time, test that resolve_market works when deadline has passed (if using a short deadline market created with already-passed deadline). Alternatively, validate the IDL-level error variant assertion: call resolve_market and verify the grace period check exists via error matching on a separate market.

8. **"Grace period expiry error exists"** -- Verify the GracePeriodExpired error variant exists in the IDL (assertion test, similar to Phase 6's approach for time-dependent tests).

Note: Full 48h time-dependent tests require Clock sysvar manipulation which is unavailable on localnet. Use IDL error variant assertion strategy established in Phase 5/6.
  </action>
  <verify>
    <automated>cd /Users/laitsky/Developments/avenir && anchor test -- --grep "resolver\|grace" 2>&1 | tail -20</automated>
  </verify>
  <done>Resolver registration, approval, staking tests pass. Grace period enforcement is verified via IDL error variant assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dispute escalation, voting, and finalization tests</name>
  <files>tests/dispute.ts</files>
  <action>
Continue in `tests/dispute.ts`, adding dispute lifecycle tests.

**Test cases (dispute escalation - RES-03):**

9. **"Market participant can open dispute"** -- Use one of the bettors to call open_dispute. Verify: Dispute PDA created with 7 jurors, dispute_tally created, market.state == Disputed(3).

10. **"Non-participant cannot open dispute"** -- Wallet with no UserPosition calls open_dispute, expect NotMarketParticipant error.

11. **"Cannot open dispute before grace period expires"** -- Attempt on a market whose deadline hasn't passed + 48h yet. Expect GracePeriodNotExpired error (or IDL assertion if time-dependent).

**Test cases (encrypted voting - RES-05):**

12. **"Init dispute tally comp def"** -- Call init_dispute_tally_comp_def. Verify comp def account created.

13. **"Init dispute tally with encrypted zeros"** -- Call init_dispute_tally MPC. Await callback. Verify DisputeTally has non-zero ciphertexts (encrypted zeros) and nonce set.

14. **"Juror casts encrypted vote"** -- One of the selected jurors calls cast_vote with encrypted vote ciphertext. Verify dispute.votes_submitted bitfield updated, dispute.vote_count incremented.

15. **"Non-juror cannot vote"** -- Wallet not in dispute.jurors calls cast_vote, expect NotSelectedJuror error.

16. **"Juror cannot vote twice"** -- Same juror calls cast_vote again, expect AlreadyVoted error.

17. **"Submit votes until quorum"** -- Have 5 jurors cast votes (reaching quorum of 5). Await MPC callbacks between each vote to ensure lock is released.

**Test cases (finalization - RES-06):**

18. **"Init finalize dispute comp def"** -- Call init_finalize_dispute_comp_def.

19. **"Finalize dispute reveals outcome"** -- Call finalize_dispute. Await callback. Verify: market.winning_outcome set (1 or 2), market.state == Resolved(2), dispute.status == Settled(2).

20. **"Market proceeds through normal payout pipeline"** -- After dispute resolves market, call compute_payouts then claim_payout to verify the standard pipeline works.

**MPC test helpers:**
- Reuse or adapt MPC helper patterns from existing test files (Phase 3 test helpers for cipher encryption, computation awaiting, etc.).
- For encrypted vote: use `cipher.encrypt(is_yes_value)` with juror's x25519 keypair.
- Await MPC callback completion between sequential vote submissions.

**Note:** If MPC tests are blocked by DKG (as noted in STATE.md), create the test structure with clear TODO comments for MPC-dependent sections, and validate the instruction-level logic (PDA creation, state transitions, error handling) without MPC execution.
  </action>
  <verify>
    <automated>cd /Users/laitsky/Developments/avenir && anchor test -- --grep "dispute\|vote\|finalize" 2>&1 | tail -20</automated>
  </verify>
  <done>Dispute escalation, encrypted voting, and finalization tests pass (or are structured with clear DKG-dependent TODOs). Full lifecycle from escalation through payout is validated.</done>
</task>

</tasks>

<verification>
1. `anchor test` passes for all dispute tests (or non-MPC tests pass, MPC tests noted as DKG-blocked)
2. Test file covers: resolver registration, approval, staking, dispute escalation, voting, finalization
3. At least 12 test cases covering happy paths and error cases
4. PDA derivations match the program's seed patterns
5. Test setup creates sufficient resolvers (7+) for jury selection
</verification>

<success_criteria>
- Resolver pool CRUD tests pass (register, approve, stake)
- Dispute escalation tests validate participant eligibility and grace period
- Encrypted voting tests validate juror eligibility, one-vote, and quorum
- Finalization test validates market resolution from dispute outcome
- Full lifecycle test: register resolvers -> escalate -> vote -> finalize -> compute_payouts -> claim_payout
- All tests compile and execute
</success_criteria>

<output>
After completion, create `.planning/phases/08-dispute-system/08-05-SUMMARY.md`
</output>
