---
phase: 06-resolution-payouts
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - tests/resolution.ts
  - Anchor.toml
autonomous: true
requirements: [RES-01, RES-02, RES-07, RES-08, RES-09]

must_haves:
  truths:
    - "resolve_market succeeds when called by creator after deadline with valid outcome"
    - "resolve_market rejects non-creator, pre-deadline, non-Open, locked market, and invalid outcome"
    - "compute_payouts MPC reveals pool totals and transitions market to Finalized(4)"
    - "claim_payout distributes correct proportional USDC with 2% fee deducted"
    - "claim_payout rejects losers, double claims, and claims on non-Finalized markets"
    - "Edge case: all bets on one side returns original bet minus fee"
  artifacts:
    - path: "tests/resolution.ts"
      provides: "Integration tests for resolve_market, compute_payouts, claim_payout"
      contains: "resolve_market"
  key_links:
    - from: "tests/resolution.ts"
      to: "tests/mpc/helpers.ts"
      via: "imports setupArciumContext, createTestMarket, getArciumAccounts, initCompDef, encryptBetInput, awaitAndVerifyCallback"
      pattern: "from.*helpers"
    - from: "tests/resolution.ts"
      to: "programs/avenir/src/lib.rs"
      via: "calls resolveMarket, computePayouts, computePayoutsCallback, claimPayout methods"
      pattern: "program\\.methods\\.(resolveMarket|computePayouts|claimPayout)"
---

<objective>
Comprehensive integration tests for the resolution and payout flow: resolve_market, compute_payouts MPC, and claim_payout.

Purpose: Validates the full lifecycle -- market creation, bet placement, resolution, MPC pool reveal, winner payout with fee, and all error cases. Tests run on Arcium localnet with actual MPC execution.

Output: tests/resolution.ts with 8+ test cases covering happy path, validation errors, and edge cases.
</objective>

<execution_context>
@/Users/laitsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laitsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-resolution-payouts/06-CONTEXT.md
@.planning/phases/06-resolution-payouts/06-RESEARCH.md
@.planning/phases/06-resolution-payouts/06-03-SUMMARY.md

<interfaces>
<!-- Test patterns from existing test files -->

From tests/place-bet.ts (test setup pattern):
- Uses program, provider, admin, configPda from anchor setup
- Creates USDC mint, funds users with tokens
- Uses createTestMarket, initCompDef, setupArciumContext, getArciumAccounts from helpers
- Sequential test accumulation (tests share market state)
- Awaits MPC callback between stateful tests

From tests/mpc/helpers.ts:
```typescript
export async function setupArciumContext(provider, programId): Promise<ArciumContext>
export function encryptBetInput(mxePublicKey, isYes, amount): EncryptedBetInput
export async function awaitAndVerifyCallback(provider, computationOffset, programId, timeout): Promise<string>
export async function createTestMarket(program, admin, creator, usdcMint, configPda, marketId, options?): Promise<TestMarketResult>
export function getArciumAccounts(programId, arciumCtx, computationOffset, compDefName): ArciumAccounts
export async function initCompDef(program, payer, compDefName, arciumCtx): Promise<void>
```

From CONTEXT.md (edge case policies -- LOCKED):
- All bets on winning side (losing_pool = 0): winners get original bet back minus 2% fee
- Rounding: floor individual payouts, dust stays in market vault
- Losers: explicit rejection with NoWinningPosition
- No claim expiry
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create resolution integration test file with full lifecycle tests</name>
  <files>tests/resolution.ts, Anchor.toml</files>
  <behavior>
    - Test 1 (Setup): Initialize config, whitelist creator, create market with short resolution time (now + 5s), init pool via MPC, init comp_defs for update_pool and compute_payouts
    - Test 2 (Place bets): Two users place bets -- User A bets 10 USDC on Yes, User B bets 5 USDC on No. Await MPC callbacks between bets.
    - Test 3 (resolve_market - NotMarketCreator): Non-creator attempts resolve, expect NotMarketCreator error
    - Test 4 (resolve_market - MarketNotExpired): Creator attempts resolve before deadline, expect MarketNotExpired error. NOTE: Use IDL error variant assertion pattern (same as Phase 5 05-04) since Clock sysvar manipulation is unavailable on localnet. Alternatively, if the market was created with a short deadline that has already passed by test execution time, this test should use a separate market with a future deadline.
    - Test 5 (resolve_market - InvalidOutcome): Creator resolves with winning_outcome=3, expect InvalidOutcome error
    - Test 6 (resolve_market - success): Creator resolves with winning_outcome=1 (Yes wins). Verify market.state == 2, market.winning_outcome == 1
    - Test 7 (compute_payouts): Anyone triggers compute_payouts on the Resolved market. Await MPC callback. Verify market.state == 4 (Finalized), market.revealed_yes_pool == 10_000_000 (10 USDC), market.revealed_no_pool == 5_000_000 (5 USDC)
    - Test 8 (claim_payout - winner): User A claims payout. Verify: gross = 10M * 15M / 10M = 15M, fee = 15M * 200 / 10000 = 300K, net = 14.7M. Check User A's token balance increased by 14.7M. Check fee_recipient token balance increased by 300K. Check position.claimed == true.
    - Test 9 (claim_payout - loser rejected): User B attempts claim, expect NoWinningPosition error (User B bet on No, but Yes won)
    - Test 10 (claim_payout - double claim): User A attempts claim again, expect AlreadyClaimed error
  </behavior>
  <action>
1. Create `tests/resolution.ts`:
   - Import from helpers.ts: setupArciumContext, createTestMarket, getArciumAccounts, initCompDef, encryptBetInput, awaitAndVerifyCallback
   - Import anchor, PublicKey, Keypair, SystemProgram, createMint, createAccount, mintTo, getAccount, TOKEN_PROGRAM_ID from standard packages
   - Import getComputationAccAddress from @arcium-hq/client

   **Test setup (before all):**
   - Set up provider, program, admin
   - Create USDC mint
   - Create two user keypairs (userA, userB) with funded SOL and USDC token accounts
   - Create fee_recipient keypair with USDC token account
   - Initialize Config (with fee_recipient and fee_bps=200)
   - Whitelist creator
   - Setup Arcium context
   - Init comp_defs: "init_pool", "update_pool", "compute_payouts"

   **Market creation:**
   - Create market with resolution_time = now + 5 seconds (short deadline for testing)
   - Init pool via init_pool MPC (await callback)
   - Place bet A: userA bets 10 USDC on Yes via place_bet (await callback)
   - Place bet B: userB bets 5 USDC on No via place_bet (await callback)
   - Wait for deadline to pass (sleep ~6 seconds)

   **resolve_market tests:**
   - Test NotMarketCreator: non-creator calls resolveMarket, assert error contains "NotMarketCreator" or IDL error code
   - Test InvalidOutcome: creator calls resolveMarket with winning_outcome=3, assert error
   - Test success: creator calls resolveMarket with winning_outcome=1
   - Fetch market and verify state=2, winning_outcome=1

   **compute_payouts test:**
   - Call computePayouts (permissionless -- use any payer)
   - Await MPC callback (awaitAndVerifyCallback)
   - Fetch market and verify: state=4, revealed_yes_pool=10_000_000, revealed_no_pool=5_000_000, mpc_lock=false

   **claim_payout tests:**
   - Winner claim: userA calls claimPayout
     - Derive UserPosition PDA for userA
     - Call with winner_token_account, market_vault, fee_recipient_token_account
     - Fetch userA token account: balance should increase by 14_700_000 (15M gross - 300K fee)
     - Fetch fee_recipient token account: balance should increase by 300_000
     - Fetch position: claimed == true
   - Loser claim: userB calls claimPayout, expect NoWinningPosition error
   - Double claim: userA calls claimPayout again, expect AlreadyClaimed error

   **Important test patterns:**
   - For resolve_market: need creator to sign. Market PDA derived with seeds [b"market", market_id.to_le_bytes()]
   - For compute_payouts: needs full Arcium accounts (same as update_pool but with "compute_payouts" comp_def)
   - For claim_payout: no MPC involvement -- pure on-chain instruction
   - Sequential test execution -- each test builds on state from previous
   - Use `await new Promise(r => setTimeout(r, 6000))` to wait for deadline to pass
   - For MarketNotExpired test: either use a separate market with future deadline, or assert the IDL error variant

2. Update `Anchor.toml` to include the new test file in the test scripts section (add `resolution = "bun test tests/resolution.ts"` or ensure it's picked up by the glob pattern).
   - Check existing Anchor.toml test configuration first. If it uses a glob like `tests/**/*.ts`, the new file is already included. If it lists files explicitly, add `tests/resolution.ts`.

3. Run the test (with Arcium localnet running):
   - `anchor test --skip-deploy --skip-build` or the appropriate test command
   - If Arcium localnet is not available, verify compilation with `bun run tsc --noEmit tests/resolution.ts` or similar
  </action>
  <verify>
    <automated>cd /Users/laitsky/Developments/avenir && anchor build 2>&1 | tail -5 && echo "---" && bun run tsc --noEmit 2>&1 | head -20 || echo "TypeScript check skipped"</automated>
  </verify>
  <done>tests/resolution.ts created with 8+ test cases covering: resolve_market (success + 3 error cases), compute_payouts MPC lifecycle, claim_payout (winner success + loser rejection + double claim). All test assertions match CONTEXT.md payout formula. Test file compiles.</done>
</task>

</tasks>

<verification>
1. tests/resolution.ts exists and contains describe/it blocks for all test cases
2. Test covers resolve_market validation: NotMarketCreator, MarketNotExpired/IDL assertion, InvalidOutcome
3. Test covers compute_payouts MPC: queue, await callback, verify revealed pool values
4. Test covers claim_payout: winner receives correct amount, fee sent to recipient, loser rejected, double claim rejected
5. Payout math in test assertions: gross=15M, fee=300K, net=14.7M (for 10 USDC Yes bet, 5 USDC No bet, Yes wins, 2% fee)
6. `anchor build` still passes (test file doesn't break compilation)
</verification>

<success_criteria>
- tests/resolution.ts has 8+ test cases
- Test assertions use exact amounts matching the payout formula
- MPC callback await pattern used for compute_payouts (same as update_pool tests)
- Sequential test structure with shared state (market created once, bets placed, then resolution + claims)
- Error assertions for all rejection cases
</success_criteria>

<output>
After completion, create `.planning/phases/06-resolution-payouts/06-04-SUMMARY.md`
</output>
